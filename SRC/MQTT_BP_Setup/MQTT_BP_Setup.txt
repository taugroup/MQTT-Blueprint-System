MQTT_BP_Setup>
>Data>
->MQTT_blueprint_system>
-->Config>(Unreal Plugin Config)
-->Content>
--->MQTT>
---->MQTT_Client.uasset
---->MQTT_Data_Import.uasset
---->MQTT_Data_Structure.uasset
-->Resources>
--->MQTT BP Guide>(My Extra Outside-Unreal Functions)
--->MQTT_Python>(My Extra Outside-Unreal Functions)
--->Icon128.png(My edited icon for the plugin)
->MQTT_blueprint_system.uplugin
->packaged(This should be the location the blueprint is built in)
-->Empty(The location inside "packaged" folder for build location)
>controlPanel.txt (Working and shouldn't be touched, except when edited by script)
>embedPlugin.py (Working and shouldn't be touched)
>enableTemplate.py (Not working, needs to be fixed)
>runEmbed.bat (Working and shouldn't be touched)
>runEnable.bat (Working and shouldn't be touched)
>seedTemplate.py (Needs functionality to migrate plugin's MQTT folder (and its contents) inside Unreal, specifically from the built plugin from embedPlugin.py)


The intended step-by-step goal of this is that the user edits controlPanel.txt
and fills out the required parameters for embedPlugin.py and enableTemplate.py

The user then launches runEmbed.bat to run embedPlugin.py, this python script
then tells Unreal to build the plugin "MQTT_blueprint_system" found in the 
Data folder, in its subfolder "packaged" once built it transfers over to the
users Unreal Engine Plugin's Folder

After that the user then launches runEnable.bat which runs the python script
enableTemplate.py, which this script then enables the plugin for the users
specified Unreal Project, and copys over the MQTT_BP_Guide folder and all its
contents, and the MQTT_Python folder and all of its contents to the users
unreal project in their projects root folder (so in the same folder as their 
.uproject), then runs seedTemplate.py

seedTemplate.py is suppose to then migrate the MQTT folder (with its contents)
found in the built and transfered over MQTT_blueprint_system plugin from their 
Unreal Engine's Plugin Folder, it can't "copy/paste" it needs to migrate 
otherwise inside Unreal the dependancies and references do not transfer over),
when the user migrates manually it works, so migrate needs to be used

There is other functionality to these scripts like changing "Was_Packaged" and
debug/fail safe related features and should be kept functional too


I need the scripts not working to work and the scripts that don't have functionality
to have the desired functionality